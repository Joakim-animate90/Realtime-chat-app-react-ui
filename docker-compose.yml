version: '3'

services:
  scan-web-one:
    build:
      context: ./
      dockerfile: ./Dockerfile
    image: Realtime-chat-app-react-ui:v1
    container_name: Realtime-chat-app-react-ui
    restart: always
    ports:
      - 3000:3000
